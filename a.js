//input
//write function อินพุตที่สามารถยกกำลัง 1/2 แล้วไม่มีเศษ
//return number that can be เลขยกกำลัง 2 พร้อมคำว่า power


// function calculateNumber(input) {
//     const result = Math.pow(input, 1/n);
//     if (Number.isInteger(result)) {
//         return "power";
//     } 
// }

// console.log(calculateNumber(8));

// console.log(Math.pow(8, 1/2));

// const number = 18;

// function calculateNumber(input) {
//     //convert number to binary
//     binary = input.toString(2)
//     for (let index = 0; index < binary.length; index++) {
//         //1 and 0 mush have to string
//         if (binary[0] === '1' && (binary[index+1] === '0')) {
//                 return 'power'
//             };
//         }
//     }

// console.log(calculateNumber(6))

// function calculateNumber(input) {
//     //convert number to binary
//     binary = input.toString(2)
//     for (let index = 0; index < binary.length; index++) {
//         //1 and 0 mush have to string
//         if (binary[0] === '1' && (binary[index+1] === '0')) {
//                 for (index in binary) {
//                     return (binary[index]);
//             };
//         }
//     }
//     return [index-1]
    
// }

